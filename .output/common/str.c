#include <stdio.h>
#include<string.h>

int main ()
{
    char s[1000];
    
    printf("Enter the string:\n");
    scanf("%[^\n]s",&s);

    for (int i = 0; i < s[i] != '\0'; i++)
    {
        if(s[i] == ' ' && s[i+1] != ' ')
        {
           
        }
    }
    
    
}